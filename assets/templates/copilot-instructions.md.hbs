# Copilot Repository Instructions (Generated)

Generated at: `{{GENERATED_AT_UTC}}`
Bridge mode: `{{BRIDGE_MODE}}`

This repository uses a shared skills ecosystem managed by `aeco` CLI.

## Zero-Setup Session Bootstrap
Run once per session before implementation work:
- `{{READY_COMMAND}}`
- For bleeding-edge source refresh: `aeco ready --latest`

## Repository Defaults ({{REPO_NAME}})
- Architecture: {{ARCHITECTURE_SUMMARY}}
- Key roots: {{KEY_ROOTS_MARKDOWN}}
- Read first: {{READ_FIRST_MARKDOWN}}
- {{CRITICAL_BEHAVIOR_HINT}}

## How to route tasks
1. Read `docs/ai/skills-index.md` for available namespaced skills.
2. Prefer namespaced skills (`oa-*`, `ag-*`) over ad-hoc prompting when a skill clearly matches.
3. If no direct match exists, use `find-skill.prompt.md` to map intent to the closest skills.
4. Keep implementation grounded in repo conventions and required test gates.

## Catalog summary
- Total skills: `{{TOTAL_SKILLS}}`
- OpenAI skills: `{{OPENAI_COUNT}}`
- Antigravity skills: `{{ANTIGRAVITY_COUNT}}`
- Index JSON: `{{INDEX_JSON_PATH}}`

## Routing rules
- `oa-*`: official OpenAI curated skills (trust tier: official-openai)
- `ag-*`: Antigravity community skills (trust tier: community)
- If both exist for same alias, prefer `oa-*` unless user asks for `ag-*` behavior.

## Required Validation Before Completion
- Backend-only changes: `{{BACKEND_VALIDATION_COMMAND}}`
- Frontend-only changes: `{{FRONTEND_VALIDATION_COMMAND}}`
- Behavior-sensitive changes: {{BEHAVIOR_VALIDATION_HINT}}
- If any validation is skipped, explicitly report why and the risk.

## Commit and Change Boundaries
- {{COMMIT_BOUNDARY_HINT}}
- If a feature requires both layers, keep tasks separated and traceable to the same spec story.

## Safety
- Respect `security.allow_exec_files` policy from `skills.manifest.yaml`.
- If a selected skill contains scripts or download commands, confirm intent before risky execution.
- {{DOC_UPDATE_HINT}}

## Useful prompts
- `/.github/prompts/find-skill.prompt.md`
- `/.github/prompts/use-skill.prompt.md`
{{SPECFLOW_SECTION}}
