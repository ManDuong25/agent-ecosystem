---
applyTo: "backend/**"
---

# Backend Instructions (Generated)

- Keep domain logic inside backend feature modules and avoid leaking cross-layer concerns.
- Shared infrastructure should stay in backend shared/core directories.
- Prefer explicit schema validation at API boundaries.
- Backend validation command: `{{BACKEND_VALIDATION_COMMAND}}`
- Keep API compatibility unless change is explicitly specified and documented.
- {{DOC_UPDATE_HINT}}